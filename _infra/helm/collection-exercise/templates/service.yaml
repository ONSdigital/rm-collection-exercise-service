apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  type: ClusterIp
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.container.port }}
    nodePort: {{ .Values.nodePort.port }}
    protocol: TCP
  selector:
    app: {{ .Chart.Name }}
